{
  "rewrites": [
    { "source": "/auth/(.*)", "destination": "/auth" },
    { "source": "/check-password/(.*)", "destination": "/check-password" },
    { "source": "/upload/(.*)", "destination": "/upload" }
]
}